name: Deploy

on:
  push:
    branches: [main]

  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: setup node
        uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: build ui
        working-directory: dnd-web
        run: |
          npm ci
          npm test -- --watchAll=false
          npm run build

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4

      - name: Deploy to Vercel
        run: npx vercel deploy ${{ github.ref == 'refs/heads/main' && '--prod' || '' }} --yes --token=${{ secrets.VERCEL_TOKEN }}
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

  ui-tests:
    runs-on: ubuntu-latest
    needs: deploy
    defaults:
      run:
        working-directory: dnd-web-tests
    steps:
      - uses: actions/checkout@v4

      - name: install dependencies
        run: npm ci

      - name: install browsers
        run: npx playwright install --with-deps firefox

      - name: run ui tests
        run: npx playwright test --project=firefox --grep=@prod
        env:
          BASE_URL: ${{ vars.VERCEL_URL }}
